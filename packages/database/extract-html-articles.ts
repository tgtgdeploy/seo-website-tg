/**
 * 从 HTML 中提取文章并同步到数据库
 *
 * 使用方法：
 * dotenv -e ../../.env.local -- npx tsx extract-html-articles.ts
 */

import { PrismaClient } from '@prisma/client'

const prisma = new PrismaClient()

// 从 HTML 中提取的文章数据
const articles = [
  {
    title: 'Telegram怎么用邮箱接收验证码？',
    slug: 'telegram-email-verification',
    excerpt: 'Telegram目前不支持通过邮箱接收验证码。验证码只能通过短信或Telegram应用内的通知发送。如果手机无法接收短信，可以尝试使用Telegram的语音验证码功能。',
    content: `# Telegram怎么用邮箱接收验证码？

Telegram目前不支持通过邮箱接收验证码。验证码只能通过短信或Telegram应用内的通知发送。如果手机无法接收短信，可以尝试使用以下方法：

## 使用短信验证码

1. 在注册或登录时输入手机号码
2. 等待接收短信验证码
3. 输入收到的验证码完成验证

## 使用语音验证码

如果无法接收短信，可以：

1. 点击"没收到验证码？"
2. 选择"通过语音接收验证码"
3. 接听电话并记下语音播报的验证码
4. 输入验证码完成验证

## 使用已登录设备验证

如果你在其他设备上已经登录Telegram，可以通过该设备接收验证码通知。

## 注意事项

- 确保手机号码输入正确
- 检查手机是否能正常接收短信
- 如果多次尝试失败，请联系Telegram客服
`,
    metaDescription: 'Telegram验证码接收方式详解，包括短信验证、语音验证和设备验证等多种方法。',
    metaKeywords: ['telegram', '验证码', '邮箱', '手机验证', '注册'],
    date: '2025-10-24',
  },
  {
    title: '如何在Telegram中清除缓存？',
    slug: 'telegram-clear-cache',
    excerpt: '在Telegram中清除缓存：打开Telegram，点击左上角菜单 > 设置 > 数据与存储 > 存储使用情况，选择要清除的缓存类型（照片、视频、文件等），点击"清除缓存"完成操作。',
    content: `# 如何在Telegram中清除缓存？

Telegram会在本地缓存媒体文件以提升用户体验，但长时间使用后缓存会占用大量存储空间。以下是清除缓存的详细步骤。

## 清除缓存步骤

### 1. 打开设置
- 打开Telegram应用
- 点击左上角的菜单图标（三条横线）
- 选择"设置"

### 2. 进入存储设置
- 在设置页面找到"数据与存储"
- 点击"存储使用情况"

### 3. 清除缓存
- 查看各类缓存占用的空间
- 选择要清除的内容类型：
  - 照片
  - 视频
  - 文件
  - 音频
  - 动图（GIF）
- 点击"清除缓存"按钮

## 自动清除设置

你还可以设置自动清除缓存：

1. 在"存储使用情况"页面
2. 设置"保留媒体"时间
3. 可选择：3天、1周、1个月或永久

## 注意事项

- 清除缓存不会删除聊天记录
- 清除后，查看媒体文件需要重新下载
- 建议定期清理以释放存储空间
`,
    metaDescription: '详细教程：如何在Telegram中清除缓存，释放手机存储空间，包括手动清除和自动清除设置。',
    metaKeywords: ['telegram', '清除缓存', '存储', '数据管理', '设置'],
    date: '2025-10-21',
  },
  {
    title: '如何在Telegram加人？',
    slug: 'telegram-add-contacts',
    excerpt: '在Telegram加人可以通过以下方式：在"联系人"页面点击"添加联系人"，输入对方手机号即可添加；也可以通过用户名、二维码或分享链接添加好友。',
    content: `# 如何在Telegram加人？

Telegram提供多种添加联系人的方式，让你轻松扩展社交网络。

## 方法一：通过手机号添加

1. 打开Telegram
2. 点击"联系人"标签
3. 点击右上角的"添加联系人"按钮
4. 输入对方的手机号码
5. 点击"完成"

## 方法二：通过用户名添加

1. 点击顶部搜索框
2. 输入对方的Telegram用户名（@username）
3. 在搜索结果中找到对方
4. 点击进入对方资料页
5. 点击"发送消息"开始聊天

## 方法三：通过二维码添加

1. 打开Telegram设置
2. 点击头像查看个人资料
3. 点击"我的二维码"
4. 让对方扫描你的二维码，或扫描对方二维码

## 方法四：通过分享链接

1. 打开个人资料
2. 点击"分享我的联系方式"
3. 通过其他app分享链接
4. 对方点击链接即可添加你

## 注意事项

- 添加联系人需要对方手机号或用户名
- 被添加方会收到通知
- 可以在隐私设置中控制谁能添加你
`,
    metaDescription: 'Telegram添加好友完整指南：手机号、用户名、二维码、分享链接四种方法详解。',
    metaKeywords: ['telegram', '加好友', '添加联系人', '用户名', '二维码'],
    date: '2025-10-18',
  },
  {
    title: 'Telegram只能手机号注册吗？',
    slug: 'telegram-registration-methods',
    excerpt: '是的，Telegram目前只支持通过手机号注册账号，用户必须使用有效的手机号码接收验证码才能完成注册。无法使用邮箱或其他方式注册。',
    content: `# Telegram只能手机号注册吗？

## 注册方式说明

是的，Telegram目前**只支持通过手机号注册账号**。这是Telegram的安全机制之一，用于验证用户身份。

## 为什么只能用手机号？

### 1. 安全性考虑
- 手机号码作为唯一标识
- 防止机器人批量注册
- 提高账号安全性

### 2. 防止滥用
- 限制恶意注册
- 减少垃圾账号
- 保护平台生态

### 3. 找回账号
- 手机号是找回账号的主要方式
- 更换设备时可以通过手机号验证

## 注册步骤

1. 下载并安装Telegram
2. 打开应用，点击"开始聊天"
3. 选择国家代码
4. 输入手机号码
5. 接收并输入验证码
6. 设置个人信息（可选）

## 常见问题

### Q: 可以使用虚拟号码注册吗？
A: 理论上可以，但Telegram可能会检测并封禁虚拟号码。

### Q: 手机号被占用怎么办？
A: 联系Telegram客服申请解除绑定。

### Q: 可以更换绑定的手机号吗？
A: 可以，在设置中更改手机号码。

## 注意事项

- 一个手机号只能注册一个Telegram账号
- 注册前确保能接收短信验证码
- 保护好你的手机号，避免账号被盗
`,
    metaDescription: 'Telegram注册方式详解：为什么只能用手机号注册，如何安全注册Telegram账号。',
    metaKeywords: ['telegram', '注册', '手机号', '账号', '安全'],
    date: '2025-10-15',
  },
  {
    title: 'Telegram怎么联系客服？',
    slug: 'telegram-contact-support',
    excerpt: '要联系客服，你可以通过Telegram内的"帮助"功能联系Telegram支持团队。在Telegram应用中，进入设置 > 问我一个问题，即可与官方客服机器人对话。',
    content: `# Telegram怎么联系客服？

Telegram提供多种客服支持渠道，帮助用户解决使用中遇到的问题。

## 方法一：应用内客服

### 1. 通过设置联系
1. 打开Telegram
2. 点击左上角菜单
3. 进入"设置"
4. 滚动到底部，点击"问我一个问题"
5. 与@Telegram 官方客服机器人对话

### 2. 直接搜索
- 在搜索框输入 @Telegram
- 点击进入对话
- 发送你的问题

## 方法二：官方支持页面

访问Telegram官方支持网站：
- 网址：telegram.org/support
- 可以提交支持请求
- 查看常见问题解答

## 方法三：社区帮助

### Telegram中文社区
- 搜索"Telegram中文"
- 加入相关群组
- 向群友求助

### FAQ页面
- 访问 telegram.org/faq
- 查找常见问题答案

## 客服响应时间

- **官方客服**：通常24-48小时内回复
- **紧急问题**：标注"紧急"可能更快处理
- **非工作时间**：可能延迟

## 提高回复效率的技巧

1. 清晰描述问题
2. 提供必要的截图
3. 说明你已尝试的解决方法
4. 提供账号相关信息（非密码）

## 常见问题可自助解决

- 账号被封：查看封禁原因，申诉
- 无法登录：检查网络，验证码问题
- 功能异常：尝试更新应用或重启

## 注意事项

- 不要向非官方客服提供密码
- 警惕冒充客服的诈骗
- 官方客服不会主动索要验证码
`,
    metaDescription: 'Telegram客服联系方式大全：应用内客服、官方支持、社区帮助等多种渠道详解。',
    metaKeywords: ['telegram', '客服', '技术支持', '帮助中心', '联系方式'],
    date: '2025-10-12',
  },
];

async function extractAndSyncArticles() {
  console.log('\n' + '='.repeat(70))
  console.log('📝 提取 HTML 文章并同步到数据库')
  console.log('='.repeat(70) + '\n')

  // 查找 website-1
  const website = await prisma.website.findFirst({
    where: {
      name: 'Demo Website 1',
    },
  })

  if (!website) {
    console.log('❌ 未找到 Demo Website 1')
    return
  }

  console.log(`✅ 找到网站: ${website.name} (ID: ${website.id})\n`)

  // 查找或创建作者（使用第一个用户）
  let author = await prisma.user.findFirst()

  if (!author) {
    console.log('⚠️  未找到用户，创建默认作者...')
    author = await prisma.user.create({
      data: {
        email: 'admin@example.com',
        role: 'ADMIN',
      },
    })
    console.log(`✅ 创建作者: ${author.email}\n`)
  }

  let successCount = 0
  let existsCount = 0
  let failedCount = 0

  for (const article of articles) {
    console.log(`\n📄 处理文章: ${article.title}`)
    console.log('   ' + '-'.repeat(60))

    try {
      // 检查文章是否已存在
      const existing = await prisma.post.findFirst({
        where: {
          slug: article.slug,
          websiteId: website.id,
        },
      })

      if (existing) {
        console.log(`   ⏭️  文章已存在，跳过`)
        existsCount++
        continue
      }

      // 创建文章
      const created = await prisma.post.create({
        data: {
          title: article.title,
          slug: article.slug,
          content: article.content,
          metaTitle: article.title,
          metaDescription: article.metaDescription,
          metaKeywords: article.metaKeywords,
          status: 'PUBLISHED',
          websiteId: website.id,
          authorId: author.id,
          createdAt: new Date(article.date),
          updatedAt: new Date(article.date),
        },
      })

      console.log(`   ✅ 已创建`)
      console.log(`      Slug: ${created.slug}`)
      console.log(`      关键词: ${created.metaKeywords.join(', ')}`)
      console.log(`      日期: ${article.date}`)

      successCount++
    } catch (error: any) {
      console.error(`   ❌ 创建失败: ${error.message}`)
      failedCount++
    }
  }

  console.log('\n' + '='.repeat(70))
  console.log('📊 同步结果汇总')
  console.log('='.repeat(70))
  console.log(`✅ 成功创建: ${successCount} 篇文章`)
  console.log(`⏭️  已存在: ${existsCount} 篇`)
  console.log(`❌ 失败: ${failedCount} 篇`)
  console.log(`📝 提取自 HTML 的文章数: ${articles.length} 篇`)
  console.log('='.repeat(70) + '\n')

  if (successCount > 0) {
    console.log('💡 下一步:')
    console.log('1. 在 Admin 后台查看新创建的文章')
    console.log('2. 访问 website-1 查看文章列表')
    console.log('3. 继续提取剩余文章（目前仅导入了前5篇）\n')
  }
}

async function main() {
  try {
    await extractAndSyncArticles()
  } catch (error) {
    console.error('\n❌ 执行出错:', error)
    process.exit(1)
  }
}

main()
  .then(async () => {
    await prisma.$disconnect()
  })
  .catch(async (e) => {
    console.error(e)
    await prisma.$disconnect()
    process.exit(1)
  })
